# 📌 Extract Digit in Python

এই README তে আমরা **Extract Digit** (একটি সংখ্যার প্রতিটি অংক আলাদা করা) বিষয়টা পরিষ্কারভাবে শিখবো। এখানে থাকবে:

* ✅ Problem Explanation
* ✅ Algorithm (Step by Step)
* ✅ Python Code
* ✅ Example & Output
* ✅ Important Notes

---

## 🔍 Problem Explanation

ধরা যাক, আমাদের কাছে একটি সংখ্যা আছে:

```
153
```

আমরা চাই এটাকে আলাদা আলাদা অংকে ভাগ করতে:

```
1
5
3
```

এটাকেই বলে **Extract Digit**। সাধারণত এটি `while loop` এবং `%` (modulus) অপারেটর ব্যবহার করে করা হয়।

---

## 🧠 Algorithm (Digit Extraction)

**Input:** একটি পূর্ণসংখ্যা `n`

**Output:** সংখ্যাটির প্রতিটি digit আলাদা করে দেখানো

### 📑 Steps:

1. Start
2. একটি সংখ্যা `n` নাও
3. যতক্ষণ `n > 0`

   * `digit = n % 10` (শেষ digit বের করো)
   * digit প্রিন্ট করো
   * `n = n // 10` (শেষ digit বাদ দাও)
4. End

---

## 🧾 Python Code (While Loop ব্যবহার করে)

```python
n = int(input("Enter a number: "))

while n > 0:
    digit = n % 10
    print(digit)
    n = n // 10
```

---

## ▶️ Example Run

**Input:**

```
153
```

**Output:**

```
3
5
1
```

⚠️ লক্ষ্য করো: Output টা **reverse order** এ আসে। কারণ আমরা ডান দিক (last digit) থেকে digit বের করছি।

---

## 🔄 Correct Order এ Digit দেখাতে চাইলে

### 🧠 Logic

* প্রথমে digit গুলো একটা list এ রাখবো
* তারপর list টা reverse করে প্রিন্ট করবো

### 🧾 Code:

```python
n = int(input("Enter a number: "))
digits = []

while n > 0:
    digits.append(n % 10)
    n = n // 10

for d in reversed(digits):
    print(d)
```

---

## 📌 Key Notes

* `% 10` → শেষ digit বের করে
* `// 10` → শেষ digit বাদ দেয়
* `while loop` → digit extraction এর জন্য সবচেয়ে বেশি ব্যবহার হয়

---

## 🎯 Where It’s Used?

* Number problems
* Palindrome check
* Armstrong number
* Digit sum
* Reverse number

---

## ✅ Conclusion

Extract Digit হলো Python এর একটি **basic কিন্তু খুব গুরুত্বপূর্ণ** concept। Loop এবং arithmetic অপারেশন ভালোভাবে বুঝতে এটা খুব কাজে লাগে।

Happy Coding 🚀
