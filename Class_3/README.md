# DSA Class 3

## list time complexity
- list.copy - Opration legth. So list length is list time complexity O(n)
- list.append - Opration one. So list time complexity O(1). 
- list.pop - Opration one. So list time complexity O(1).
- list.pop intermidite - Opration length. So lisy time complexity O(n).
